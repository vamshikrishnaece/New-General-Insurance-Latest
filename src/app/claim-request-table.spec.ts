import { ClaimRequestTable } from './claim-request-table';

describe('ClaimRequestTable', () => {
  it('should create an instance', () => {
    expect(new ClaimRequestTable()).toBeTruthy();
  });
});
